spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: admin
      name: api-gateway
      label: main
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 5000
        max-attempts: 6
        multiplier: 1.1
